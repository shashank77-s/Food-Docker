services:

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      MONGO_URI: mongodb+srv://shashank:Shashank%40123@cluster0.dlcrt7a.mongodb.net/foodapp
      JWT_SECRET: mysecretkey
      ADMIN_SECRET_CODE: myadminsecret123